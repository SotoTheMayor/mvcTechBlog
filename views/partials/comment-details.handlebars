<div class="comment">
    <div class="row comment-text">
            {{comment}}
        </div>
        <div class="row comment-user justify-content-end">
            -- {{user.username}} -- {{timestamp}}
            <button class="btn btn-primary" type="delete" id="comment-delete">Delete</button>
        </div>
    </div>
</div>